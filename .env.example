# Linear personal API key — powers the live roadmap sync on /roadmap.
# Create one at: Linear → Settings → Security & access → Personal API keys.
# Without it, the site falls back to the static roadmap in lib/roadmap-config.ts.
LINEAR_API_KEY=

# Anthropic API key — powers the Dawkins demo chat (existing).
ANTHROPIC_API_KEY=
